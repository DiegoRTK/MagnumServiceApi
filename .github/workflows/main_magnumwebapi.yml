# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - magnumwebapi

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://magnumapitest.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_43feaef32b77448a9feab8df2fb58db1 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_5590cdbcf2994b4f9d9adfa6ea6985d7 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: magnumapitest.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_43feaef32b77448a9feab8df2fb58db1 }}/magnum-api:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'magnumwebapi'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_a80439a78424456a9298ac834df7f46e }}
        images: 'magnumapitest.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_43feaef32b77448a9feab8df2fb58db1 }}/magnum-api:${{ github.sha }}'